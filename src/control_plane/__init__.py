"""Enterprise AI Control Plane.

A modular platform for running enterprise AI in production: organizations & access control,
an AI gateway (model routing + cost metering), a prompt registry, an evaluation engine, and
observability. Built as a modular monolith — clean module boundaries in one deployable — and
designed to absorb the author's existing flagships (observability, agents, product analytics)
as first-class modules over time. See ARCHITECTURE.md.
"""

__version__ = "0.1.0"

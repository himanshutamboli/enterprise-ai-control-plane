"""Operator dashboard: a platform-admin (cross-organization) view over the control plane.

`data.py` holds pure query functions and a demo seed — no UI imports, so it's testable in CI.
The Streamlit UI lives in the repo-root `dashboard.py`.
"""
